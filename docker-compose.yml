version: "3.9"

services:
  web:
    build: .
    container_name: cuckoo-local
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    env_file: .env
    environment:
      - DATABASE_URL=sqlite:////app/data/db.sqlite3
      - FLASK_ENV=production